name: Safe Crawler Check

on:
  pull_request:
    paths:
      - "src/safe-crawler/**"
      - "tests/safe-crawler.test.cjs"
      - "tsconfig.safe-crawler.json"
      - "tsconfig.safe-crawler-test.json"
      - "package.json"
      - ".github/workflows/safe-crawler-check.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  security-and-tests:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Install dependencies safely
        run: npm install --ignore-scripts

      - name: Audit dependencies
        run: npm run audit:security

      - name: Type-check safe crawler
        run: npx tsc --noEmit -p tsconfig.safe-crawler.json

      - name: Run safe crawler security tests
        run: npm run test:safe-crawler
